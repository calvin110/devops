version: '3.1'
services:
  jenkins:
    restart: always
    image: jenkinsci/blueocean
    container_name: jenkins
    ports:
      # 发布端口
      - 8080:8080
      # 基于 JNLP 的 Jenkins 代理通过 TCP 端口 50000 与 Jenkins master 进行通信
      - 50000:50000
    environment:
      TZ: Asia/Shanghai
    volumes:
      - /us/local/docker/Jenkins/data:/var/jenkins_home
